import React from 'react';
import './styles/global.scss';
import { AppRouter } from './AppRouter';

const App = () => {
    {
        return (
            <AppRouter />
        );
    }
};

export default App;
